// JSONLoader.swift — placeholder, will be implemented in sdd-apply.
// Pure function: read a path from disk, decode JSON into [SessionState].
// Surface errors as a typed Result so the caller can render empty/error states.
import Foundation

enum JSONLoader {
    // TODO: implement load(from:) -> Result<[SessionState], Error>
}

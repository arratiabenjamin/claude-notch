// SessionRow.swift — placeholder, will be implemented in sdd-apply.
// Renders a single SessionState as a row in the floating panel.
import SwiftUI

struct SessionRow: View {
    var body: some View {
        // TODO: real row layout (project name, status, last-activity, indicator)
        EmptyView()
    }
}

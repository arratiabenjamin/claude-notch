// EmptyStateView.swift — placeholder, will be implemented in sdd-apply.
// Shown when there are zero active Claude Code sessions.
import SwiftUI

struct EmptyStateView: View {
    var body: some View {
        // TODO: friendly empty state copy + subtle illustration
        EmptyView()
    }
}
